A small App Router demo of **streaming Server Components** with `<Suspense>`.

Each child waits a different number of milliseconds. Fallbacks show first; faster holes resolve without waiting for slower ones.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
